# Swift / SwiftPM
.build/
.swiftpm/
Package.resolved
build/

# Xcode
*.xcodeproj/

# Generated by XcodeGen from project.yml (single source of truth)
App/Info.plist
App/*.entitlements
Complication/Info.plist
Complication/*.entitlements
Container/Info.plist
*.xcworkspace/
xcuserdata/
DerivedData/
*.xcuserstate

# Screenshots are hosted as GitHub release assets (the `assets` release), not
# committed, so they don't bloat history. Kept locally for re-uploading.
images/screenshots/

# macOS
.DS_Store

# Local Claude Code settings
.claude/settings.local.json
.llm/
