// Template for Config/Secrets.xcconfig, which is git-ignored because this repo
// is public. `just generate` copies this file there if it doesn't exist yet.
//
// AIRNOW_API_KEY powers the air quality complication — request a free key at
// https://docs.airnowapi.org and paste it into Config/Secrets.xcconfig (not
// here). Left empty, everything still builds and runs; the AQI feature just
// stays off (no AQI line in the app, a dash in the complication).
AIRNOW_API_KEY =
